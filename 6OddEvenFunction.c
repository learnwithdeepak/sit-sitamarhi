#include<stdio.h>
int main ()
{
    int num;
    printf("Enter any integer\n");
    scanf("%d",&num);
    if (num%2==0)
    {
    printf("%d is an even number.",num);
    }
    else
    {
    printf("%d is a odd number",num);
    }
   return 0; 
}

//Write a program to check the entered number is ODD or EVEN
// Codes written by Deepak and tested in VS CODE